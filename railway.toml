[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "sh -c 'langflow run --host 0.0.0.0 --port ${PORT:-7860}'"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10